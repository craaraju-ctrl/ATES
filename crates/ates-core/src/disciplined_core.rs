use chrono::{DateTime, Utc, Timelike};

// ... (keeping previous full implementation) ...

pub fn get_discipline_summary() -> &'static str {
    "Disciplined Core v0.2 – Pivot points, session timing, risk limits, and confluence active"
}