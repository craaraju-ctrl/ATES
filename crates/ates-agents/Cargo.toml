[package]
name = "ates-agents"
version = "0.1.0"
edition = "2021"

[dependencies]
ates-core = { path = "../ates-core" }
ates-autonomous = { path = "../ates-autonomous" }
async-trait = "0.1"
log = "0.4"
chrono = { version = "0.4", features = ["serde"] }