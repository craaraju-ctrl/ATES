[package]
name = "ates-orchestrator"
version = "0.1.0"
edition = "2021"

[dependencies]
ates-core = { path = "../ates-core" }
ates-agents = { path = "../ates-agents" }
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
serde_json = "1.0"